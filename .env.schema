SQLITE_CHARACTERS='absolute path to local sqlite file. quotes aren't mandatory despite webstorm warnings'
SQLITE_SKINS='absolute path to local sqlite file. quotes aren't mandatory despite webstorm warnings'
L2D_FILE='absolute path to local json file. quotes aren't mandatory despite webstorm warnings'